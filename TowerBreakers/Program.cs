namespace TowerBreakers;

public class Program
{

public static int towerBreakers(int n, int m)
    {
        bool turns = true;
        int[] towers;
        
        for (int i = 0; i < n; i++)
        {
            towers.Append(m);
        }
        
        while (true)
        {
            if (turns)
        }
    }
}